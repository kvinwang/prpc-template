[template]
exclude = ["cargo-generate.toml", "hooks"]
[hooks]
post = ["hooks/post-gen.rhai"]

[placeholders]
app_name = { prompt = "Enter the app name", type = "string" }
type_name = { prompt = "Enter the server type name", type = "string" }

