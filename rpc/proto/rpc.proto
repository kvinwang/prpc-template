syntax = "proto3";

import "google/protobuf/empty.proto";

package {{app_name}};

service {{app_name | capitalize}} {
  // Example request
  rpc HandleRequest(google.protobuf.Empty) returns (google.protobuf.Empty) {}
}
