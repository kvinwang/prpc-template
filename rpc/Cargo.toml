[package]
name = "{{app_name}}-rpc"
version = "0.1.0"
edition = "2021"

[dependencies]
prpc = "0.3.0"
prost = "0.13.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1"

[build-dependencies]
prpc-build = "0.3.2"
